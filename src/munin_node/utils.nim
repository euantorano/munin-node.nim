## Utility methods for working with munin connections.

const
  Version* = "0.1.0"
    ## The version number of the application.

  VersionString* = "Munin Node for Windows " & Version
    ## The version string sent when a "version" command is received by the server.
